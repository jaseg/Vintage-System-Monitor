Vintage multimeters used as system monitors to display cpu load etc.

The hardware is a pretty straightforward ATMega8 with some low-pass filters on its
two 16-bit-PWM-outputs and the usual software-USB-stuff (resistors, connector,
12MHz crystal).
